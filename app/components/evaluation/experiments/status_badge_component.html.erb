<turbo-frame id="experiment-status" <%= "src=\"#{turbo_src}\"".html_safe if turbo_src %> <%= "refresh=\"interval\" data-turbo-refresh-interval=\"3000\"".html_safe if polling? %>>
  <%= render UI::StatusBadgeComponent.new(label: @experiment.status, variant: badge_variant) %>
</turbo-frame>
