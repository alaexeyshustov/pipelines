<%= render UI::PageHeaderComponent.new(title: "New Pipeline", parent: { label: "Pipelines", url: orchestration_pipelines_path }) %>

<div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 max-w-2xl">
  <%= render "form", pipeline: @pipeline %>
</div>
