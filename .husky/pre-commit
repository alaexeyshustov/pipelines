echo "Building JS assets before push..."
pnpm run build

echo "Building CSS assets before push..."
bundle exec rails tailwindcss:build

echo "#check signatures are up to date"
bundle exec sentinel check